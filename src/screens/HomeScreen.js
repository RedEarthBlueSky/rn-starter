import React from "react"
import { Text, StyleSheet } from "react-native"
import { screenStyles } from "../styles/styles"

const { textStyle } = screenStyles

const HomeScreen = () => {
  return <Text style={textStyle}>SociaLite Home Screen!</Text>
};

export { HomeScreen }
